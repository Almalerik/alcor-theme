jQuery(document).ready(function() {
	
	//Header widget
	wp.customize.section('sidebar-widgets-sidebar-header').panel('alcor_header');
	wp.customize.section('sidebar-widgets-sidebar-header').priority('200');

});
